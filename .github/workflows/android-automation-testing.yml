name: Android Automation Testing

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  android-automation-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Prevent hanging workflows
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: |
        echo "Installing dependencies (deterministic)..."

        # Use npm ci when a lockfile exists for reproducible installs, otherwise fallback to npm install
        if [ -f package-lock.json ]; then
          echo "Lockfile found: using npm ci --legacy-peer-deps"
          npm ci --legacy-peer-deps || {
            echo "ERROR: npm ci failed"; tail -50 ~/.npm/_logs/*.log 2>/dev/null || true; exit 1;
          }
        else
          echo "No lockfile found: using npm install --legacy-peer-deps"
          npm install --legacy-peer-deps || {
            echo "ERROR: npm install failed"; tail -50 ~/.npm/_logs/*.log 2>/dev/null || true; exit 1;
          }
        fi

        echo "✅ Dependencies installed successfully"

        # Ensure critical Expo modules are installed
        echo "🔧 Ensuring critical Expo modules are installed..."
        export npm_config_legacy_peer_deps=true
        
        # Install expo-modules-core and expo-modules-autolinking explicitly with --save flag
        echo "📦 Installing expo-modules-core and expo-modules-autolinking..."
        npm install expo-modules-core expo-modules-autolinking --legacy-peer-deps --save || {
          echo "⚠️ npm install with --save failed, trying without --save...";
          npm install expo-modules-core expo-modules-autolinking --legacy-peer-deps || {
            echo "❌ Both npm install attempts failed";
            echo "Listing package.json dependencies:";
            cat package.json | grep -A 20 "dependencies" || true;
            exit 1;
          }
        }
        
        echo "✅ npm install completed successfully"
        
        # Verify packages are in node_modules BEFORE expo install
        echo "🔍 Verifying packages in node_modules..."
        if [ ! -d "node_modules/expo-modules-core" ]; then
          echo "❌ ERROR: expo-modules-core not found after npm install"
          echo "Listing node_modules/expo-* directories:"
          ls -la node_modules/expo-* 2>/dev/null || echo "No expo modules found"
          exit 1
        fi
        
        if [ ! -d "node_modules/expo-modules-autolinking" ]; then
          echo "❌ ERROR: expo-modules-autolinking not found after npm install"
          echo "This is required for Gradle plugin resolution"
          echo "Listing node_modules/expo-* directories:"
          ls -la node_modules/expo-* 2>/dev/null || echo "No expo modules found"
          exit 1
        fi
        
        echo "✅ Both packages verified in node_modules"
        
        # Then run expo install for version compatibility (non-fatal if fails)
        echo "📦 Running expo install for version compatibility..."
        CI=1 npx expo install expo-modules-core expo-modules-autolinking || {
          echo "⚠️ WARNING: expo install failed, but packages already installed via npm";
          echo "Continuing with npm-installed versions...";
        }

        # Final verification after expo install
        echo "🔍 Final verification of expo modules..."
        if [ ! -d "node_modules/expo-modules-core" ]; then
          echo "❌ ERROR: expo-modules-core disappeared after expo install"
          exit 1
        fi
        
        if [ ! -d "node_modules/expo-modules-autolinking" ]; then
          echo "❌ ERROR: expo-modules-autolinking disappeared after expo install"
          exit 1
        fi

        if [ ! -d "node_modules/expo-modules-core/android" ]; then
          echo "⚠️ WARNING: expo-modules-core/android directory not found"
          echo "Directory contents:"
          ls -la node_modules/expo-modules-core/ || true
          echo "📋 Package info:"
          cat node_modules/expo-modules-core/package.json | grep -A5 '"version"' || true
          echo "⚠️ Continuing despite missing android directory - may be present after prebuild"
        else
          echo "✅ expo-modules-core Android files verified"
        fi
        
        echo "✅ All critical Expo modules verified"
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Enable KVM group perms
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm
        
    - name: Cache AVD
      uses: actions/cache@v4
      id: avd-cache
      with:
        path: |
          ~/.android/avd/*
          ~/.android/adb*
        key: avd-29
        
    - name: Create AVD and generate snapshot for caching
      if: steps.avd-cache.outputs.cache-hit != 'true'
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 29
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        force-avd-creation: false
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
        disable-animations: false
        script: echo "Generated AVD snapshot for caching."
        
    - name: Generate Android Project
      run: |
        echo "🔧 Building native Android project..."
        export CI=true
        export EXPO_NO_TELEMETRY=1
        
        # Verify expo-modules-autolinking is installed (critical for Gradle plugins)
        echo "🔍 Verifying expo-modules-autolinking..."
        if [ ! -d "node_modules/expo-modules-autolinking" ]; then
          echo "⚠️ expo-modules-autolinking missing, installing..."
          npm install expo-modules-autolinking --legacy-peer-deps
        fi
        
        # Clean any previous Android build
        rm -rf android
        
        # Run prebuild with verbose logging
        echo "🚀 Running expo prebuild..."
        npx expo prebuild --platform android --clean || {
          echo "❌ ERROR: expo prebuild failed"
          echo "📋 Checking expo-modules-core gradle files:"
          ls -la node_modules/expo-modules-core/android/ || echo "android directory not found"
          exit 1
        }
        
        # Verify project structure
        echo "🔍 Verifying Android project structure..."
        test -d "android" || (echo "❌ ERROR: android directory not created" && ls -la && exit 1)
        test -f "android/build.gradle" || (echo "❌ ERROR: android/build.gradle missing" && exit 1)
        test -f "android/app/build.gradle" || (echo "❌ ERROR: android/app/build.gradle missing" && exit 1)
        test -f "android/settings.gradle" || (echo "❌ ERROR: android/settings.gradle missing" && exit 1)
        
        # Verify Gradle plugin files exist
        echo "🔍 Verifying expo-module-gradle-plugin..."
        GRADLE_PLUGIN_PATH="node_modules/expo-modules-core/android"
        if [ -f "$GRADLE_PLUGIN_PATH/build.gradle" ]; then
          echo "✅ Found expo-modules-core Gradle plugin at: $GRADLE_PLUGIN_PATH"
        else
          echo "⚠️ WARNING: expo-modules-core Gradle files not complete"
          ls -la "$GRADLE_PLUGIN_PATH" || true
        fi
        
        # Check settings.gradle for expo-modules-core inclusion
        echo "🔍 Checking settings.gradle for expo-modules-core..."
        grep -n "expo-modules-core" android/settings.gradle || {
          echo "⚠️ WARNING: expo-modules-core not found in settings.gradle"
        }
        
        echo "✅ Android project generated successfully"
        echo "📁 Android directory contents:"
        ls -la android/

    - name: Build Android APK
      run: |
        echo "🏗️ Building Android APK..."
        
        # Clear Gradle cache to prevent stale plugin issues
        echo "🧹 Clearing Gradle cache..."
        rm -rf ~/.gradle/caches/
        rm -rf android/.gradle/
        
        cd android
        
        # Make gradlew executable
        chmod +x gradlew
        
        # Verify Gradle wrapper
        echo "🔍 Verifying Gradle wrapper..."
        ./gradlew --version || {
          echo "❌ ERROR: Gradle wrapper not working"
          exit 1
        }
        
        # List all included projects to verify expo-modules-core is found
        echo "🔍 Listing Gradle projects..."
        ./gradlew projects || true
        
        # Build with info logging
        echo "🚀 Building APK..."
        ./gradlew assembleDebug --info --stacktrace || {
          echo "❌ ERROR: Gradle build failed"
          echo "📋 Checking settings.gradle:"
          cat settings.gradle || true
          echo "📋 Checking app/build.gradle:"
          head -50 app/build.gradle || true
          exit 1
        }
        
        cd ..
        echo "✅ APK build completed"
        
    - name: Verify APK Creation
      run: |
        APK_PATH="android/app/build/outputs/apk/debug/app-debug.apk"
        
        echo "🔍 Looking for APK at: $APK_PATH"
        
        if [ ! -f "$APK_PATH" ]; then
          echo "❌ ERROR: APK not found at: $APK_PATH"
          echo "🔍 Directory structure:"
          ls -R android/app/build/outputs/ 2>/dev/null || echo "build/outputs directory not found"
          echo "🔍 Searching for any APK files..."
          find android -name "*.apk" -type f
          exit 1
        fi
        
        echo "✅ APK found at: $APK_PATH"
        ls -lh "$APK_PATH"

    - name: Upload APK artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk

    - name: Download APK artifact
      uses: actions/download-artifact@v4
      with:
        name: app-debug-apk
        path: ./android-app-apk

    - name: Setup Android Emulator and Install APK
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 29
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        force-avd-creation: false
        emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
        emulator-boot-timeout: 900
        disable-animations: true
        script: |
          set -e
          echo "🚀 Starting Android emulator"
          echo "⏳ Waiting for Android emulator to be ready..."
          adb wait-for-device
          sleep 30
          echo "📱 Checking emulator status..."
          adb devices -l
          echo "📦 Preparing to install APK..."
          APK_DOWNLOAD_PATH="./android-app-apk/app-debug.apk"
          APK_BUILT_PATH="android/app/build/outputs/apk/debug/app-debug.apk"
          if [ -f "$APK_DOWNLOAD_PATH" ]; then APK="$APK_DOWNLOAD_PATH"; else APK="$APK_BUILT_PATH"; fi
          echo "📦 Installing APK: $APK"
          if [ ! -f "$APK" ]; then echo "❌ ERROR: APK not found at: $APK"; echo "🔍 Directory listing (downloaded):"; ls -R ./android-app-apk 2>/dev/null || true; echo "🔍 Directory listing (built):"; ls -R android/app/build/outputs/ 2>/dev/null || true; exit 1; fi
          adb install "$APK"
          echo "✅ Verifying app installation..."
          adb shell pm list packages | grep com.voyager.rn || true

    - name: Setup Automation Testing
      run: |
        echo "🔧 Setting up automation environment (automation/)..."
        cd automation
        # Prefer deterministic installs in CI using the lockfile. Fallback to npm install
        if [ -f package-lock.json ]; then
          npm ci --legacy-peer-deps || npm install --legacy-peer-deps
        else
          npm install --legacy-peer-deps
        fi

        # Use the project-local Appium (via npx) instead of installing globally.
        # This matches iOS workflow changes and avoids global ESM/version conflicts.
        echo "🔧 Installing Appium drivers via project-local appium (npx)"
        npx appium driver install uiautomator2 || {
          echo "⚠️ Warning: appium driver install failed; attempting to continue";
        }
        cd ..

    - name: Run Android Automation Tests  
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 29
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        force-avd-creation: false
        emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
        disable-animations: true
        script: |
          echo "🚀 Starting Appium server and running tests..."
          
          # Start Appium server in background
          appium --log-level info &
          APPIUM_PID=$!
          sleep 15
          
          # Verify Appium is running
          curl -f http://localhost:4723/status || {
            echo "❌ ERROR: Appium server not responding"
            exit 1
          }
          
          # Run automation tests
          cd automation
          echo "🧪 Running Android automation tests..."
          PLATFORM=android npx wdio run wdio.mobile.conf.ts --spec tests/mobile/travel-preferences-success.test.ts
          
          # Cleanup
          echo "🧹 Cleaning up Appium..."
          kill $APPIUM_PID || true
        
    - name: Kill crashpad_handler processes (Fix for emulator hanging)
      if: always()
      run: |
        echo "🧹 Cleaning up remaining processes..."
        
        # Check for running processes
        echo "Checking for crashpad_handler processes:"
        pgrep -l crashpad_handler || echo "No crashpad_handler processes found"
        
        echo "Checking for qemu processes:"
        pgrep -l qemu || echo "No qemu processes found"
        
        # Kill crashpad_handler processes (known to cause hanging)
        echo "Terminating crashpad_handler processes..."
        pkill -SIGTERM crashpad_handler 2>/dev/null || true
        sleep 5
        pkill -SIGKILL crashpad_handler 2>/dev/null || true
        
        # Also kill any remaining emulator processes
        echo "Terminating any remaining emulator processes..."
        pkill -SIGTERM qemu-system 2>/dev/null || true
        sleep 3
        pkill -SIGKILL qemu-system 2>/dev/null || true
        
        echo "✅ Process cleanup complete"
          
    - name: Capture test artifacts on failure
      if: failure()
      run: |
        echo "Capturing debug information..."
        
        # Check if any devices are available first
        echo "Checking for available devices..."
        adb devices -l || echo "No adb available"
        
        # Only attempt to capture artifacts if emulator is still running
        if adb devices | grep -q "device$"; then
          echo "Emulator still running, capturing artifacts..."
          
          # Capture emulator screenshot
          timeout 30 adb exec-out screencap -p > automation/android-screenshot.png 2>/dev/null || echo "Failed to capture screenshot"
          
          # Capture logcat
          timeout 30 adb logcat -d > automation/android-logcat.log 2>/dev/null || echo "Failed to capture logcat"
          
          # List installed packages for debugging
          timeout 30 adb shell pm list packages > automation/android-packages.txt 2>/dev/null || echo "Failed to list packages"
        else
          echo "No active emulator found, skipping artifact capture"
          echo "No active emulator at time of failure" > automation/android-debug-info.txt
        fi
        
        # Always create logs directory for upload
        mkdir -p automation/logs automation/screenshots
        
    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: android-test-results-${{ github.run_number }}
        path: |
          automation/logs/
          automation/screenshots/
          automation/*.log
          automation/*.png
          automation/*.txt