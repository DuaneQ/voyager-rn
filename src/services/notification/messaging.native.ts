/**
 * Mobile-only Firebase Messaging wrapper
 * 
 * This file is automatically selected by Metro bundler on iOS/Android
 * platforms (via the .native.ts extension). Web uses messaging.ts instead.
 * 
 * @react-native-firebase/messaging provides native FCM token management.
 * On iOS, it handles APNs → FCM token mapping natively through the Firebase SDK,
 * eliminating the need for the deprecated IID batchImport API.
 */
import messaging from '@react-native-firebase/messaging';

export default messaging;
