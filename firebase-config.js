// Firebase configuration for voyager-RN (React Native Expo)
// Re-export from src/config/firebaseConfig to avoid duplicate initialization

export { app, auth, getAuthInstance, db, storage, functions } from './src/config/firebaseConfig';